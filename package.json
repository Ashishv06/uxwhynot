{
  "name": "uxwhynot",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "next dev",
    "build": "next build",
    "start": "next start",
    "postinstall": "playwright install chromium --with-deps || playwright install chromium || echo 'Playwright browser install skipped — run npx playwright install chromium manually before starting the app.'"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.32.0",
    "@axe-core/playwright": "^4.10.0",
    "better-sqlite3": "^11.3.0",
    "chrome-launcher": "^1.1.2",
    "lighthouse": "^12.2.1",
    "next": "14.2.5",
    "playwright": "1.62.1",
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  }
}
